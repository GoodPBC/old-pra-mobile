import React, { Component } from 'react';

import { StyleSheet, View, Text } from 'react-native';

export default class ServiceRequestContactSelector extends Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <Text>This is where the text selector for the service request contact number is going to go.</Text>
    );
  }
}