import React, { Component } from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default class Teams extends Component {
  constructor(props){
    super(props)
  }

  render() {
    return(
      <Text>Teams filler component</Text>
    )
  }
}